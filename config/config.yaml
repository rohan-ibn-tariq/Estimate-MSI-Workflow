# ============================================
# Estimate-MSI-Workflow - Configuration
# ============================================

# ---------------------------------------------
# Sample Selection
# ---------------------------------------------
# Which samples to process (from samples.tsv)
# Options:
#   - null: Process ALL samples in samples.tsv
#   - ["sample1", "sample2"]: Process only specified samples
samples_to_run: null

# ---------------------------------------------
# Reference Genome
# ---------------------------------------------
ref:
  species: "homo_sapiens"
  build: "GRCh38"
  release: "115"

  # Chromosome filter for testing
  # "22" = chr22 only (fast testing)
  # ""   = all chromosomes (production)
  chromosome: "22"

# ---------------------------------------------
# MSI Analysis Parameters
# ---------------------------------------------
msi:
  # MSI-High threshold (percentage)
  # Samples with MSI score >= this are classified as MSI-High
  threshold: 3.5
  
  # PyTRF minimum repeats per motif length
  # Format: [mono, di, tri, tetra, penta, hexa]
  # Example: [5, 5, 5, 5, 5, 5] means:
  #   - Mononucleotide repeats: >= 5 repeats (e.g., AAAAA)
  #   - Dinucleotide repeats: >= 5 repeats (e.g., CAGCAGCAGCAGCAG)
  #   - etc.
  min_repeats: [5, 3, 3, 3, 3, 3]

# ---------------------------------------------
# Variant Calling
# ---------------------------------------------
# Varlociraptor configuration
# NOTE: Using custom feature branch build during development.
# Create config/config.local.yaml with your local paths (see README).
# After feature is merged to main and available via conda, this section
# will be removed and varlociraptor will use conda environment instead.
varlociraptor:
  # These are placeholder paths - override in config.local.yaml
  binary: "/path/to/varlociraptor/target/debug/varlociraptor"
  directory: "/path/to/varlociraptor"
  setup_script: "/path/to/varlociraptor/setup_pixi_env.sh"
  # Scenario file for tumor-only calling
  scenario: "config/scenario.yaml"

# ---------------------------------------------
# Computational Resources
# ---------------------------------------------
threads:
  default: 4
  indexing: 8
  alignment: 14
  varlociraptor: 4
  pytrf: 2
